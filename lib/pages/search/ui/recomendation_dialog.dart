import 'package:flutter/material.dart';

class RecomendationDialog extends StatelessWidget {
  const RecomendationDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("RECOMENDATION TEXT"),
    );
  }
}
